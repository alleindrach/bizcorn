<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="allein.service.mapper.UserMapper" >

    <resultMap id="UserResultMap" type="allein.model.user.User" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="login_password" property="password" jdbcType="VARCHAR" />
        <result column="mobile" property="mobile" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="User_Column_List" >
        id, name, login_password,mobile
    </sql>
    <select id="selectById" resultMap="UserResultMap" parameterType="java.lang.Integer" >
        select
        <include refid="User_Column_List" />
        from tad_user
        where id = #{id,jdbcType=INTEGER}
    </select>
    <select id="selectByName" resultMap="UserResultMap" parameterType="java.lang.String" >
        select
        <include refid="User_Column_List" />
        from tad_user
        where name = #{name,jdbcType=VARCHAR}
    </select>
    <insert id="insert" parameterType="allein.model.user.User" >
        insert into tad_user (id, name, password,mobile
        )
        values (#{id,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR},#{mobile,jdbcType=VARCHAR}
        )
    </insert>

    <update id="update" parameterType="allein.model.user.User" >
        update tad_user
        <set >
            <if test="name != null" >
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="password != null" >
                password = #{password,jdbcType=VARCHAR},
            </if>
            <if test="mobile != null" >
                mobile = #{mobile,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

</mapper>
